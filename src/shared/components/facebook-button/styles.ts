import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        height: 45,
        width: 45,
        borderRadius: 23,
        backgroundColor: '#3B5998',
        overflow: 'hidden',
    },
});
