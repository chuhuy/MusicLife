import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: 'transparent',
    },
    touchArea: {
        paddingVertical: 5,
        paddingHorizontal: 5,
    },
});
