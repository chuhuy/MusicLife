import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    body: {
        paddingTop: 20,
        flex: 1
    },
    section: {
        marginBottom: 30,
        marginTop: -5
    },
    buttonContainer: {
        alignItems: 'center',
        marginTop: 10
    }
})